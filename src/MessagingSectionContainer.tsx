import styled from "@emotion/styled/macro";

/**
 * Contains both the messages, message input and send button.
 */
export const MessagingSectionContainer = styled.div`
  background-color: orange;
  display: flex;
  height: 100%;
  flex-direction: column;
  justify-content: flex-end;
  overflow: hidden;
`;
