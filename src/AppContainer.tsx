import styled from "@emotion/styled/macro";

/**
 * Contains the entire app, in case you didn't guess :-).
 */
export const AppContainer = styled.div`
  display: flex;
  flex-direction: column;
  height: 100vh;
  background-color: hotpink;
  z-index: 0;
  overflow: hidden;
`;
